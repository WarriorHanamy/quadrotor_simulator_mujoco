# Standalone build entry for hosts without ROS.
# Usage: cmake -B build -S .
#
# Target definitions live in deps/cmake/SimCore.cmake (shared with CMakeLists.txt).

cmake_minimum_required(VERSION 3.10)
project(quadrotor_sim_core_standalone)
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

include("${CMAKE_CURRENT_LIST_DIR}/deps/cmake/SimCore.cmake")
